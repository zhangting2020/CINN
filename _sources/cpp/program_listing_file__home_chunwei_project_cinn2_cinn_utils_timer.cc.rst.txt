
.. _program_listing_file__home_chunwei_project_cinn2_cinn_utils_timer.cc:

Program Listing for File timer.cc
=================================

|exhale_lsh| :ref:`Return to documentation for file <file__home_chunwei_project_cinn2_cinn_utils_timer.cc>` (``/home/chunwei/project/cinn2/cinn/utils/timer.cc``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   #include "cinn/utils/timer.h"
   
   namespace cinn {
   namespace utils {
   
   float Timer::Stop() {
     end_     = std::chrono::high_resolution_clock::now();
     auto ts  = std::chrono::duration_cast<std::chrono::nanoseconds>(end_ - start_);
     float ms = 1000. * static_cast<double>(ts.count()) * std::chrono::nanoseconds::period::num /
                std::chrono::nanoseconds::period::den;
     return ms;
   }
   
   void Timer::Start() { start_ = std::chrono::high_resolution_clock::now(); }
   
   }  // namespace utils
   }  // namespace cinn
