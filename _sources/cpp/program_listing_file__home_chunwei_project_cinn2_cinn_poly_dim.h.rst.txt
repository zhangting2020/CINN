
.. _program_listing_file__home_chunwei_project_cinn2_cinn_poly_dim.h:

Program Listing for File dim.h
==============================

|exhale_lsh| :ref:`Return to documentation for file <file__home_chunwei_project_cinn2_cinn_poly_dim.h>` (``/home/chunwei/project/cinn2/cinn/poly/dim.h``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   #pragma once
   
   #include <cstdint>
   #include <string>
   #include <utility>
   #include <vector>
   
   #include "cinn/ir/ir_base.h"
   
   namespace cinn {
   namespace poly {
   
   struct Dim {
     using value_t = ir::Expr;
     using range_t = std::pair<value_t, value_t>;
   
     std::string id;
     value_t lower_bound;
     value_t upper_bound;
   
     Dim(std::string id) : id(std::move(id)) {}
   
     Dim(std::string id, uint32_t lower_bound, uint32_t upper_bound)
         : id(std::move(id)), lower_bound(lower_bound), upper_bound(upper_bound) {}
   
     Dim(std::string id, ir::Expr lower_bound, ir::Expr upper_bound);
   
     range_t range() const { return std::make_pair(lower_bound, upper_bound); }
   
     bool is_param() const { return !lower_bound.defined() && !lower_bound.defined(); }
   
     std::string range_repr() const;
   };
   
   }  // namespace poly
   }  // namespace cinn
